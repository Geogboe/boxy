# v1-prerelease Planning Artifacts

This folder contains v1 planning and migration artifacts. Each file is a snapshot of a proposal or a set of implementation notes associated with v1-prerelease.

Notes:

- These are *planning* documents; they are not canonical.
- Some items have been implemented and should be converted to ADRs or canonical docs; others remain draft or archived.
- Follow the migration plan in `docs/planning/README.md` to move finalized decisions into `docs/decisions/` and implementation details into `docs/architecture/` or package READMEs.

Status: Mixed — please consult each individual file for `Status:` metadata.
