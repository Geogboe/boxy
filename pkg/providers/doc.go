// Package providers contains built-in provider driver implementations intended
// for reuse as libraries outside the Boxy runtime.
//
// Each provider kind lives in its own subpackage (docker, hyperv, ...).
package providers
