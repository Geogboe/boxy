// Package drivers contains built-in providersdk driver implementations.
//
// These implementations are meant to be reusable outside of the Boxy runtime.
// They depend on providersdk types/interfaces, but providersdk itself does not
// depend on drivers to avoid import cycles.
package drivers
