# Mock Provider

This package contains a mock implementation of the `Provider` interface. It is used exclusively for testing the core application logic without needing a real backend like Docker or Hyper-V. It simulates the behavior of a provider (e.g., delays for provisioning) but performs no actual operations.