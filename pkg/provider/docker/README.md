# Docker Provider

This package contains the implementation of the `Provider` interface for managing local Docker containers. It communicates with the local Docker daemon via the official Docker SDK for Go to create, inspect, and destroy container resources.