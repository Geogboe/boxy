---
# Allocate one container from the web pool into a sandbox.
#
# Run: boxy sandbox create demo.sandbox.yaml
name: web-sandbox
resources:
  - pool: web
    count: 1
    packages:
      - mark-allocation@1.0.0
